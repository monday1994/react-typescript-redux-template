import {
    REQUEST_IN_PROGRESS,
    REQUEST_FINISHED,
} from '../../constants/actionTypes';

export const checkUserAccessToCatalog = (userId: string, catalogId: string, entitlementName: string) => async (dispatch: Function, getState: Function) => {

};

export const fetchEntitlements = (shouldPaginate = true, shouldPaginateForward = true, searchQuery = '', size = 10) => async (dispatch: Function, getState: Function) => {

};

